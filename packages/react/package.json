{
  "name": "@markprompt/react",
  "version": "1.0.2",
  "description": "A headless React component for adding GPT-4 powered search using the Markprompt API.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/motifland/markprompt-js.git",
    "directory": "packages/react"
  },
  "license": "MIT",
  "sideEffects": false,
  "type": "module",
  "exports": "./dist/index.js",
  "files": [
    "README.md",
    "dist"
  ],
  "scripts": {
    "build": "tsc --build --clean && tsc --build",
    "dev": "tsc --build --clean && tsc --build --watch",
    "dev-pack": "npm pack; mv markprompt-react-1.0.2.tgz ../../examples/with-css-modules/",
    "prepack": "tsc --build --clean && tsc --build"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.0.3",
    "classnames": "^2.3.2",
    "client-only": "^0.0.1",
    "react-markdown": "^8.0.6",
    "remark-gfm": "^3.0.1"
  },
  "devDependencies": {
    "react": "^18.0.0"
  },
  "peerDependencies": {
    "react": "^16 || ^17 || ^18"
  },
  "publishConfig": {
    "access": "public"
  }
}
